cmake_minimum_required(VERSION 2.8)
project( grayCardTracker )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( trackGrayCard trackGrayCard.cpp )
target_link_libraries( trackGrayCard ${OpenCV_LIBS} )